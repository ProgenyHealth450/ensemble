{"id":"br-pct","title":"Fix Tier 1-2 version drift (version-locked contract)","description":"Tier 1-2 packages (core, product, development, quality, infrastructure, git, e2e-testing, metrics) have drifted to 5.3.1/5.6.0/6.0.4/5.3.0/5.3.0/5.2.1/5.2.1/5.2.1. This violates README.md:411 (\"Core plugins (Tier 1-2) maintain version synchronization\") and docs/PRD/completed/automated-version-management.md:277 (FR-3.7 \"Version-locked strategy: All 25 packages maintain same version\"). Decide target version (highest 6.0.4? align with ensemble-full 6.9.2?), update all 8 package.json + 8 .claude-plugin/plugin.json + marketplace.json entries + package-lock.json entries, refresh CHANGELOG, then verify scripts/validate-version-sync.js (if exists) or reconstruct the contract enforcement. Scope: PRDs/TRDs already document this contract; inherited from upstream pre-rebrand; not introduced by rebrand but should be cleaned up in a follow-up.","status":"open","priority":2,"issue_type":"task","created_at":"2026-08-07T16:21:31.991085Z","created_by":"ldangelo","updated_at":"2026-08-07T16:21:31.991085Z","source_repo":"ensemble","source_repo_path":"/Users/ldangelo/Development/Sunstone/ensemble","compaction_level":0,"original_size":0}
{"id":"br-651","title":"zz-probe-delete-me","description":"probe","status":"tombstone","priority":3,"issue_type":"task","created_at":"2026-08-07T20:00:39.311439700Z","created_by":"unknown","updated_at":"2026-08-07T20:00:49.585933200Z","source_repo":"ensemble","source_repo_path":"\\\\?\\C:\\dev\\ensemble","deleted_at":"2026-08-07T20:00:49.585330Z","deleted_by":"unknown","delete_reason":"delete","original_type":"task","compaction_level":0,"original_size":0}
{"id":"br-n4s","title":"[trd:trd-2026-6a87c77f-frontmatter-yaml-escaping] Implement TRD: TRD-2026-6a87c77f: YAML-Safe Frontmatter Emission in the Markdown Generator","description":"**Source PRD:** `docs/PRD/PRD-2026-6a87c77f-frontmatter-yaml-escaping.md` (v1.0.1, readiness 4.94 PASS)","status":"open","priority":2,"issue_type":"epic","created_at":"2026-08-07T20:00:50.008078300Z","created_by":"unknown","updated_at":"2026-08-07T20:00:54.829380600Z","source_repo":"ensemble","source_repo_path":"\\\\?\\C:\\dev\\ensemble","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"br-n4s","depends_on_id":"br-5d0","type":"blocks","created_at":"2026-08-07T20:00:54.829103900Z","created_by":"unknown","metadata":"{}","thread_id":""},{"issue_id":"br-n4s","depends_on_id":"br-e9u","type":"blocks","created_at":"2026-08-07T20:00:54.555578700Z","created_by":"unknown","metadata":"{}","thread_id":""}]}
{"id":"br-e9u","title":"[trd:trd-2026-6a87c77f-frontmatter-yaml-escaping:pr:1] PR 1: Correct frontmatter emission and gate it","description":"PR 1 of TRD: TRD-2026-6a87c77f: YAML-Safe Frontmatter Emission in the Markdown Generator. Contains 15 tasks. Shippable state: All 19 previously-unloadable ensemble commands and agents — including both of","status":"open","priority":2,"issue_type":"feature","created_at":"2026-08-07T20:00:50.222598300Z","created_by":"unknown","updated_at":"2026-08-07T20:00:59.064604200Z","source_repo":"ensemble","source_repo_path":"\\\\?\\C:\\dev\\ensemble","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"br-e9u","depends_on_id":"br-0cy","type":"blocks","created_at":"2026-08-07T20:00:55.416475700Z","created_by":"unknown","metadata":"{}","thread_id":""},{"issue_id":"br-e9u","depends_on_id":"br-490","type":"blocks","created_at":"2026-08-07T20:00:56.575137500Z","created_by":"unknown","metadata":"{}","thread_id":""},{"issue_id":"br-e9u","depends_on_id":"br-6uy","type":"blocks","created_at":"2026-08-07T20:00:57.962024400Z","created_by":"unknown","metadata":"{}","thread_id":""},{"issue_id":"br-e9u","depends_on_id":"br-9fi","type":"blocks","created_at":"2026-08-07T20:00:59.064358100Z","created_by":"unknown","metadata":"{}","thread_id":""},{"issue_id":"br-e9u","depends_on_id":"br-ane","type":"blocks","created_at":"2026-08-07T20:00:55.694461900Z","created_by":"unknown","metadata":"{}","thread_id":""},{"issue_id":"br-e9u","depends_on_id":"br-asl","type":"blocks","created_at":"2026-08-07T20:00:58.517049700Z","created_by":"unknown","metadata":"{}","thread_id":""},{"issue_id":"br-e9u","depends_on_id":"br-cds","type":"blocks","created_at":"2026-08-07T20:00:56.855284100Z","created_by":"unknown","metadata":"{}","thread_id":""},{"issue_id":"br-e9u","depends_on_id":"br-dtd","type":"blocks","created_at":"2026-08-07T20:00:58.790300600Z","created_by":"unknown","metadata":"{}","thread_id":""},{"issue_id":"br-e9u","depends_on_id":"br-e51","type":"blocks","created_at":"2026-08-07T20:00:56.010497Z","created_by":"unknown","metadata":"{}","thread_id":""},{"issue_id":"br-e9u","depends_on_id":"br-fg9","type":"blocks","created_at":"2026-08-07T20:00:57.408428Z","created_by":"unknown","metadata":"{}","thread_id":""},{"issue_id":"br-e9u","depends_on_id":"br-ieq","type":"blocks","created_at":"2026-08-07T20:00:55.141457100Z","created_by":"unknown","metadata":"{}","thread_id":""},{"issue_id":"br-e9u","depends_on_id":"br-ioo","type":"blocks","created_at":"2026-08-07T20:00:57.688948300Z","created_by":"unknown","metadata":"{}","thread_id":""},{"issue_id":"br-e9u","depends_on_id":"br-mb5","type":"blocks","created_at":"2026-08-07T20:00:56.293687400Z","created_by":"unknown","metadata":"{}","thread_id":""},{"issue_id":"br-e9u","depends_on_id":"br-ni5","type":"blocks","created_at":"2026-08-07T20:00:58.236643500Z","created_by":"unknown","metadata":"{}","thread_id":""},{"issue_id":"br-e9u","depends_on_id":"br-tq8","type":"blocks","created_at":"2026-08-07T20:00:57.132110Z","created_by":"unknown","metadata":"{}","thread_id":""}]}
{"id":"br-5d0","title":"[trd:trd-2026-6a87c77f-frontmatter-yaml-escaping:pr:2] PR 2: Extend the rule to the pi generator","description":"PR 2 of TRD: TRD-2026-6a87c77f: YAML-Safe Frontmatter Emission in the Markdown Generator. Contains 3 tasks. Shippable state: Agent artifacts produced by the `packages/pi` pipeline survive a description","status":"open","priority":2,"issue_type":"feature","created_at":"2026-08-07T20:00:50.449085300Z","created_by":"unknown","updated_at":"2026-08-07T20:00:59.904367700Z","source_repo":"ensemble","source_repo_path":"\\\\?\\C:\\dev\\ensemble","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"br-5d0","depends_on_id":"br-4gs","type":"blocks","created_at":"2026-08-07T20:00:59.346698700Z","created_by":"unknown","metadata":"{}","thread_id":""},{"issue_id":"br-5d0","depends_on_id":"br-d1l","type":"blocks","created_at":"2026-08-07T20:00:59.626487500Z","created_by":"unknown","metadata":"{}","thread_id":""},{"issue_id":"br-5d0","depends_on_id":"br-yht","type":"blocks","created_at":"2026-08-07T20:00:59.904117800Z","created_by":"unknown","metadata":"{}","thread_id":""}]}
{"id":"br-ieq","title":"[trd:trd-2026-6a87c77f-frontmatter-yaml-escaping:task:TRD-001] - [ ] **TRD-001** Create `scripts/lib/yaml-scalar.js` exporting `foldScalar` and `yamlScalar` (0.5h) ``","description":"## Task: TRD-001\nTRD Reference: docs/TRD/TRD-2026-6a87c77f-frontmatter-yaml-escaping.md#trd-001\nPRD Reference: docs/PRD/PRD-2026-6a87c77f-frontmatter-yaml-escaping.md#req-001\n\n--- TRD Context ---\n### 1.1 Chosen approach — Option A: `JSON.stringify` scalar helper at the existing emit seam\n\nKeep the hand-built frontmatter lines in both transformers. Introduce one helper that folds a value\nto a single line and quotes it via `JSON.stringify`, and route every emitted value through it.\nSeparately, add a parse check at `generateMarkdown()` — the single funnel every artifact already\npasses through — and reuse that same check in `validate-all.js`.\n\n```js\n// scripts/lib/yaml-scalar.js\nconst foldScalar  = (v) => String(v).replace(/\\s+/g, ' ').trim();\nconst yamlScalar  = (v) => JSON.stringify(foldScalar(v));\n```\n\n**Why `JSON.stringify`:** a JSON string literal is a valid YAML 1.2 double-quoted scalar. The\nescaping is therefore stdlib's, not ours — there is no hand-rolled quoting logic to get wrong, and\nno \"does this need quoting?\" predicate. That predicate is precisely the defect in `packages/pi`\n(PRD REQ-008).\n\n**Verified** against eight adversarial inputs — mid-string `: `, bracket group with trailing\ncontent, embedded newlines and blank lines, embedded double quotes and backslashes, a leading `*`\nwith `#`/`|`/`>`/`%`/`&`/`!` throughout, a bare `ensemble:feature`, leading/trailing whitespace,\nand non-ASCII. All eight round-trip through `yaml.load` to the folded original exactly.\n\n### 1.2 Alternatives considered\n\n**Option B — serialize the frontmatter block with `js-yaml`'s `dump()`.**\nCorrect by construction with none of our own quoting logic, and measured to produce a *smaller*\ndiff than Option A (js-yaml quotes only when necessary, so previously-valid artifacts emit\nbyte-identically). Rejected on emitter-style coupling, measured empirically:\n\n| Behaviour | `dump()` output | Consequence |\n|---|---|---|\n| Multi-line description | emits a `\\|` block scalar | valid YAML, but violates REQ-003's single-line requirement; needs pre-folding anyway |\n| `tools: [Read, Write]` | restyled to a block sequence (`- Read`) | changes every agent artifact's shape |\n| `last-updated: 2026-03-15` | newly quoted | js-yaml decides, we don't |\n\nEach is individually tunable, but the result is our code negotiating with a general-purpose\nemitter's heuristics for a seven-key flat map. Option A has no heuristics at all.\n\n> **PRD correction:** PRD v1.0.1's non-goal states `yaml.dump()` would produce \"a much noisier\n> diff.\" That rationale is wrong — measurement shows the opposite. The conclusion (don't use it)\n> still holds, on the emitter-coupling grounds above. PRD corrected to match in v1.0.2.\n\n**Option C — inline the quoting separately in each transformer.** Rejected by PRD REQ-001: three\nemitters already exist and `packages/pi`'s copy has already drifted into incorrectness. One helper,\none rule.\n\n### 1.3 Two shapes that must not be conflated\n\nThe single highest-risk detail in this change. Two frontmatter keys look similar and are not:\n\n| Key | Current emission | YAML type | Correct treatment |\n|---|---|---|---|\n| `allowed-tools` (command) | `allowed_tools.join(', ')` | **string** — `\"Read, Write\"` | `yamlScalar(joined)` — quote the whole joined string |\n| `tools` (agent) | `[${tools.join(', ')}]` | **flow sequence** — `[\"Read\",\"Write\"]` | `[${tools.map(yamlScalar).join(', ')}]` — quote each element |\n\nQuoting `tools` as a whole string would collapse a list into one string; bracketing `allowed-tools`\nwould promote a string into a list. Either silently changes tool permissions. Both are pinned by\ndedicated ACs in TRD-002 and TRD-003.\nSatisfies: REQ-001\nPRD ACs: AC-001-1, AC-001-2\nTarget File: \nActions:\n\nImplementation AC:\n\nDependencies: ","status":"open","priority":2,"issue_type":"task","created_at":"2026-08-07T20:00:50.721700400Z","created_by":"unknown","updated_at":"2026-08-07T20:00:50.721700400Z","source_repo":"ensemble","source_repo_path":"\\\\?\\C:\\dev\\ensemble","compaction_level":0,"original_size":0}
{"id":"br-0cy","title":"[trd:trd-2026-6a87c77f-frontmatter-yaml-escaping:task:TRD-001-TEST] - [ ] **TRD-001-TEST** Unit tests for `yaml-scalar` over the eight adversarial inputs (0.5h) ` `","description":"## Test Task: TRD-001-TEST\nTRD Reference: docs/TRD/TRD-2026-6a87c77f-frontmatter-yaml-escaping.md#trd-001-test\nPRD Reference: docs/PRD/PRD-2026-6a87c77f-frontmatter-yaml-escaping.md#req-001\n\n--- TRD Context ---\n### 1.1 Chosen approach — Option A: `JSON.stringify` scalar helper at the existing emit seam\n\nKeep the hand-built frontmatter lines in both transformers. Introduce one helper that folds a value\nto a single line and quotes it via `JSON.stringify`, and route every emitted value through it.\nSeparately, add a parse check at `generateMarkdown()` — the single funnel every artifact already\npasses through — and reuse that same check in `validate-all.js`.\n\n```js\n// scripts/lib/yaml-scalar.js\nconst foldScalar  = (v) => String(v).replace(/\\s+/g, ' ').trim();\nconst yamlScalar  = (v) => JSON.stringify(foldScalar(v));\n```\n\n**Why `JSON.stringify`:** a JSON string literal is a valid YAML 1.2 double-quoted scalar. The\nescaping is therefore stdlib's, not ours — there is no hand-rolled quoting logic to get wrong, and\nno \"does this need quoting?\" predicate. That predicate is precisely the defect in `packages/pi`\n(PRD REQ-008).\n\n**Verified** against eight adversarial inputs — mid-string `: `, bracket group with trailing\ncontent, embedded newlines and blank lines, embedded double quotes and backslashes, a leading `*`\nwith `#`/`|`/`>`/`%`/`&`/`!` throughout, a bare `ensemble:feature`, leading/trailing whitespace,\nand non-ASCII. All eight round-trip through `yaml.load` to the folded original exactly.\n\n### 1.2 Alternatives considered\n\n**Option B — serialize the frontmatter block with `js-yaml`'s `dump()`.**\nCorrect by construction with none of our own quoting logic, and measured to produce a *smaller*\ndiff than Option A (js-yaml quotes only when necessary, so previously-valid artifacts emit\nbyte-identically). Rejected on emitter-style coupling, measured empirically:\n\n| Behaviour | `dump()` output | Consequence |\n|---|---|---|\n| Multi-line description | emits a `\\|` block scalar | valid YAML, but violates REQ-003's single-line requirement; needs pre-folding anyway |\n| `tools: [Read, Write]` | restyled to a block sequence (`- Read`) | changes every agent artifact's shape |\n| `last-updated: 2026-03-15` | newly quoted | js-yaml decides, we don't |\n\nEach is individually tunable, but the result is our code negotiating with a general-purpose\nemitter's heuristics for a seven-key flat map. Option A has no heuristics at all.\n\n> **PRD correction:** PRD v1.0.1's non-goal states `yaml.dump()` would produce \"a much noisier\n> diff.\" That rationale is wrong — measurement shows the opposite. The conclusion (don't use it)\n> still holds, on the emitter-coupling grounds above. PRD corrected to match in v1.0.2.\n\n**Option C — inline the quoting separately in each transformer.** Rejected by PRD REQ-001: three\nemitters already exist and `packages/pi`'s copy has already drifted into incorrectness. One helper,\none rule.\n\n### 1.3 Two shapes that must not be conflated\n\nThe single highest-risk detail in this change. Two frontmatter keys look similar and are not:\n\n| Key | Current emission | YAML type | Correct treatment |\n|---|---|---|---|\n| `allowed-tools` (command) | `allowed_tools.join(', ')` | **string** — `\"Read, Write\"` | `yamlScalar(joined)` — quote the whole joined string |\n| `tools` (agent) | `[${tools.join(', ')}]` | **flow sequence** — `[\"Read\",\"Write\"]` | `[${tools.map(yamlScalar).join(', ')}]` — quote each element |\n\nQuoting `tools` as a whole string would collapse a list into one string; bracketing `allowed-tools`\nwould promote a string into a list. Either silently changes tool permissions. Both are pinned by\ndedicated ACs in TRD-002 and TRD-003.\nVerifies Task: docs/TRD/TRD-2026-6a87c77f-frontmatter-yaml-escaping.md#trd-001\nVerifies: TRD-001\nSatisfies: REQ-001\nPRD ACs Proven: AC-001-1, AC-001-2\nProof of requirement: \nTarget Files: \nActions:\n\nTest AC:\n\nDependencies: TRD-001","status":"open","priority":2,"issue_type":"task","created_at":"2026-08-07T20:00:50.935918700Z","created_by":"unknown","updated_at":"2026-08-07T20:01:00.217486900Z","source_repo":"ensemble","source_repo_path":"\\\\?\\C:\\dev\\ensemble","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"br-0cy","depends_on_id":"br-ieq","type":"blocks","created_at":"2026-08-07T20:01:00.217200400Z","created_by":"unknown","metadata":"{}","thread_id":""}]}
{"id":"br-ane","title":"[trd:trd-2026-6a87c77f-frontmatter-yaml-escaping:task:TRD-002] - [ ] **TRD-002** Route all seven command frontmatter keys through `yamlScalar`; keep `allowed-tools` a joined **string** (1h) ` `","description":"## Task: TRD-002\nTRD Reference: docs/TRD/TRD-2026-6a87c77f-frontmatter-yaml-escaping.md#trd-002\nPRD Reference: docs/PRD/PRD-2026-6a87c77f-frontmatter-yaml-escaping.md#req-002\n\n--- TRD Context ---\n### 1.1 Chosen approach — Option A: `JSON.stringify` scalar helper at the existing emit seam\n\nKeep the hand-built frontmatter lines in both transformers. Introduce one helper that folds a value\nto a single line and quotes it via `JSON.stringify`, and route every emitted value through it.\nSeparately, add a parse check at `generateMarkdown()` — the single funnel every artifact already\npasses through — and reuse that same check in `validate-all.js`.\n\n```js\n// scripts/lib/yaml-scalar.js\nconst foldScalar  = (v) => String(v).replace(/\\s+/g, ' ').trim();\nconst yamlScalar  = (v) => JSON.stringify(foldScalar(v));\n```\n\n**Why `JSON.stringify`:** a JSON string literal is a valid YAML 1.2 double-quoted scalar. The\nescaping is therefore stdlib's, not ours — there is no hand-rolled quoting logic to get wrong, and\nno \"does this need quoting?\" predicate. That predicate is precisely the defect in `packages/pi`\n(PRD REQ-008).\n\n**Verified** against eight adversarial inputs — mid-string `: `, bracket group with trailing\ncontent, embedded newlines and blank lines, embedded double quotes and backslashes, a leading `*`\nwith `#`/`|`/`>`/`%`/`&`/`!` throughout, a bare `ensemble:feature`, leading/trailing whitespace,\nand non-ASCII. All eight round-trip through `yaml.load` to the folded original exactly.\n\n### 1.2 Alternatives considered\n\n**Option B — serialize the frontmatter block with `js-yaml`'s `dump()`.**\nCorrect by construction with none of our own quoting logic, and measured to produce a *smaller*\ndiff than Option A (js-yaml quotes only when necessary, so previously-valid artifacts emit\nbyte-identically). Rejected on emitter-style coupling, measured empirically:\n\n| Behaviour | `dump()` output | Consequence |\n|---|---|---|\n| Multi-line description | emits a `\\|` block scalar | valid YAML, but violates REQ-003's single-line requirement; needs pre-folding anyway |\n| `tools: [Read, Write]` | restyled to a block sequence (`- Read`) | changes every agent artifact's shape |\n| `last-updated: 2026-03-15` | newly quoted | js-yaml decides, we don't |\n\nEach is individually tunable, but the result is our code negotiating with a general-purpose\nemitter's heuristics for a seven-key flat map. Option A has no heuristics at all.\n\n> **PRD correction:** PRD v1.0.1's non-goal states `yaml.dump()` would produce \"a much noisier\n> diff.\" That rationale is wrong — measurement shows the opposite. The conclusion (don't use it)\n> still holds, on the emitter-coupling grounds above. PRD corrected to match in v1.0.2.\n\n**Option C — inline the quoting separately in each transformer.** Rejected by PRD REQ-001: three\nemitters already exist and `packages/pi`'s copy has already drifted into incorrectness. One helper,\none rule.\n\n### 1.3 Two shapes that must not be conflated\n\nThe single highest-risk detail in this change. Two frontmatter keys look similar and are not:\n\n| Key | Current emission | YAML type | Correct treatment |\n|---|---|---|---|\n| `allowed-tools` (command) | `allowed_tools.join(', ')` | **string** — `\"Read, Write\"` | `yamlScalar(joined)` — quote the whole joined string |\n| `tools` (agent) | `[${tools.join(', ')}]` | **flow sequence** — `[\"Read\",\"Write\"]` | `[${tools.map(yamlScalar).join(', ')}]` — quote each element |\n\nQuoting `tools` as a whole string would collapse a list into one string; bracketing `allowed-tools`\nwould promote a string into a list. Either silently changes tool permissions. Both are pinned by\ndedicated ACs in TRD-002 and TRD-003.\nSatisfies: REQ-002\nPRD ACs: AC-002-1, AC-002-2, AC-003-1, AC-003-2\nTarget File: \nActions:\n\nImplementation AC:\n\nDependencies: TRD-001","status":"open","priority":2,"issue_type":"task","created_at":"2026-08-07T20:00:51.134863900Z","created_by":"unknown","updated_at":"2026-08-07T20:01:00.517934900Z","source_repo":"ensemble","source_repo_path":"\\\\?\\C:\\dev\\ensemble","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"br-ane","depends_on_id":"br-ieq","type":"blocks","created_at":"2026-08-07T20:01:00.517618600Z","created_by":"unknown","metadata":"{}","thread_id":""}]}
{"id":"br-e51","title":"[trd:trd-2026-6a87c77f-frontmatter-yaml-escaping:task:TRD-002-TEST] - [ ] **TRD-002-TEST** Unit tests for command frontmatter emission (0.5h) ` `","description":"## Test Task: TRD-002-TEST\nTRD Reference: docs/TRD/TRD-2026-6a87c77f-frontmatter-yaml-escaping.md#trd-002-test\nPRD Reference: docs/PRD/PRD-2026-6a87c77f-frontmatter-yaml-escaping.md#req-002\n\n--- TRD Context ---\n### 1.1 Chosen approach — Option A: `JSON.stringify` scalar helper at the existing emit seam\n\nKeep the hand-built frontmatter lines in both transformers. Introduce one helper that folds a value\nto a single line and quotes it via `JSON.stringify`, and route every emitted value through it.\nSeparately, add a parse check at `generateMarkdown()` — the single funnel every artifact already\npasses through — and reuse that same check in `validate-all.js`.\n\n```js\n// scripts/lib/yaml-scalar.js\nconst foldScalar  = (v) => String(v).replace(/\\s+/g, ' ').trim();\nconst yamlScalar  = (v) => JSON.stringify(foldScalar(v));\n```\n\n**Why `JSON.stringify`:** a JSON string literal is a valid YAML 1.2 double-quoted scalar. The\nescaping is therefore stdlib's, not ours — there is no hand-rolled quoting logic to get wrong, and\nno \"does this need quoting?\" predicate. That predicate is precisely the defect in `packages/pi`\n(PRD REQ-008).\n\n**Verified** against eight adversarial inputs — mid-string `: `, bracket group with trailing\ncontent, embedded newlines and blank lines, embedded double quotes and backslashes, a leading `*`\nwith `#`/`|`/`>`/`%`/`&`/`!` throughout, a bare `ensemble:feature`, leading/trailing whitespace,\nand non-ASCII. All eight round-trip through `yaml.load` to the folded original exactly.\n\n### 1.2 Alternatives considered\n\n**Option B — serialize the frontmatter block with `js-yaml`'s `dump()`.**\nCorrect by construction with none of our own quoting logic, and measured to produce a *smaller*\ndiff than Option A (js-yaml quotes only when necessary, so previously-valid artifacts emit\nbyte-identically). Rejected on emitter-style coupling, measured empirically:\n\n| Behaviour | `dump()` output | Consequence |\n|---|---|---|\n| Multi-line description | emits a `\\|` block scalar | valid YAML, but violates REQ-003's single-line requirement; needs pre-folding anyway |\n| `tools: [Read, Write]` | restyled to a block sequence (`- Read`) | changes every agent artifact's shape |\n| `last-updated: 2026-03-15` | newly quoted | js-yaml decides, we don't |\n\nEach is individually tunable, but the result is our code negotiating with a general-purpose\nemitter's heuristics for a seven-key flat map. Option A has no heuristics at all.\n\n> **PRD correction:** PRD v1.0.1's non-goal states `yaml.dump()` would produce \"a much noisier\n> diff.\" That rationale is wrong — measurement shows the opposite. The conclusion (don't use it)\n> still holds, on the emitter-coupling grounds above. PRD corrected to match in v1.0.2.\n\n**Option C — inline the quoting separately in each transformer.** Rejected by PRD REQ-001: three\nemitters already exist and `packages/pi`'s copy has already drifted into incorrectness. One helper,\none rule.\n\n### 1.3 Two shapes that must not be conflated\n\nThe single highest-risk detail in this change. Two frontmatter keys look similar and are not:\n\n| Key | Current emission | YAML type | Correct treatment |\n|---|---|---|---|\n| `allowed-tools` (command) | `allowed_tools.join(', ')` | **string** — `\"Read, Write\"` | `yamlScalar(joined)` — quote the whole joined string |\n| `tools` (agent) | `[${tools.join(', ')}]` | **flow sequence** — `[\"Read\",\"Write\"]` | `[${tools.map(yamlScalar).join(', ')}]` — quote each element |\n\nQuoting `tools` as a whole string would collapse a list into one string; bracketing `allowed-tools`\nwould promote a string into a list. Either silently changes tool permissions. Both are pinned by\ndedicated ACs in TRD-002 and TRD-003.\nVerifies Task: docs/TRD/TRD-2026-6a87c77f-frontmatter-yaml-escaping.md#trd-002\nVerifies: TRD-002\nSatisfies: REQ-002\nPRD ACs Proven: AC-002-1, AC-002-2, AC-003-1, AC-003-2\nProof of requirement: \nTarget Files: \nActions:\n\nTest AC:\n\nDependencies: TRD-002","status":"open","priority":2,"issue_type":"task","created_at":"2026-08-07T20:00:51.342160400Z","created_by":"unknown","updated_at":"2026-08-07T20:01:00.807277900Z","source_repo":"ensemble","source_repo_path":"\\\\?\\C:\\dev\\ensemble","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"br-e51","depends_on_id":"br-ane","type":"blocks","created_at":"2026-08-07T20:01:00.807051700Z","created_by":"unknown","metadata":"{}","thread_id":""}]}
{"id":"br-mb5","title":"[trd:trd-2026-6a87c77f-frontmatter-yaml-escaping:task:TRD-003] - [ ] **TRD-003** Route agent frontmatter through `yamlScalar`; keep `tools` a **flow sequence** with quoted elements (0.5h) ` `","description":"## Task: TRD-003\nTRD Reference: docs/TRD/TRD-2026-6a87c77f-frontmatter-yaml-escaping.md#trd-003\nPRD Reference: docs/PRD/PRD-2026-6a87c77f-frontmatter-yaml-escaping.md#req-002\n\n--- TRD Context ---\n### 1.1 Chosen approach — Option A: `JSON.stringify` scalar helper at the existing emit seam\n\nKeep the hand-built frontmatter lines in both transformers. Introduce one helper that folds a value\nto a single line and quotes it via `JSON.stringify`, and route every emitted value through it.\nSeparately, add a parse check at `generateMarkdown()` — the single funnel every artifact already\npasses through — and reuse that same check in `validate-all.js`.\n\n```js\n// scripts/lib/yaml-scalar.js\nconst foldScalar  = (v) => String(v).replace(/\\s+/g, ' ').trim();\nconst yamlScalar  = (v) => JSON.stringify(foldScalar(v));\n```\n\n**Why `JSON.stringify`:** a JSON string literal is a valid YAML 1.2 double-quoted scalar. The\nescaping is therefore stdlib's, not ours — there is no hand-rolled quoting logic to get wrong, and\nno \"does this need quoting?\" predicate. That predicate is precisely the defect in `packages/pi`\n(PRD REQ-008).\n\n**Verified** against eight adversarial inputs — mid-string `: `, bracket group with trailing\ncontent, embedded newlines and blank lines, embedded double quotes and backslashes, a leading `*`\nwith `#`/`|`/`>`/`%`/`&`/`!` throughout, a bare `ensemble:feature`, leading/trailing whitespace,\nand non-ASCII. All eight round-trip through `yaml.load` to the folded original exactly.\n\n### 1.2 Alternatives considered\n\n**Option B — serialize the frontmatter block with `js-yaml`'s `dump()`.**\nCorrect by construction with none of our own quoting logic, and measured to produce a *smaller*\ndiff than Option A (js-yaml quotes only when necessary, so previously-valid artifacts emit\nbyte-identically). Rejected on emitter-style coupling, measured empirically:\n\n| Behaviour | `dump()` output | Consequence |\n|---|---|---|\n| Multi-line description | emits a `\\|` block scalar | valid YAML, but violates REQ-003's single-line requirement; needs pre-folding anyway |\n| `tools: [Read, Write]` | restyled to a block sequence (`- Read`) | changes every agent artifact's shape |\n| `last-updated: 2026-03-15` | newly quoted | js-yaml decides, we don't |\n\nEach is individually tunable, but the result is our code negotiating with a general-purpose\nemitter's heuristics for a seven-key flat map. Option A has no heuristics at all.\n\n> **PRD correction:** PRD v1.0.1's non-goal states `yaml.dump()` would produce \"a much noisier\n> diff.\" That rationale is wrong — measurement shows the opposite. The conclusion (don't use it)\n> still holds, on the emitter-coupling grounds above. PRD corrected to match in v1.0.2.\n\n**Option C — inline the quoting separately in each transformer.** Rejected by PRD REQ-001: three\nemitters already exist and `packages/pi`'s copy has already drifted into incorrectness. One helper,\none rule.\n\n### 1.3 Two shapes that must not be conflated\n\nThe single highest-risk detail in this change. Two frontmatter keys look similar and are not:\n\n| Key | Current emission | YAML type | Correct treatment |\n|---|---|---|---|\n| `allowed-tools` (command) | `allowed_tools.join(', ')` | **string** — `\"Read, Write\"` | `yamlScalar(joined)` — quote the whole joined string |\n| `tools` (agent) | `[${tools.join(', ')}]` | **flow sequence** — `[\"Read\",\"Write\"]` | `[${tools.map(yamlScalar).join(', ')}]` — quote each element |\n\nQuoting `tools` as a whole string would collapse a list into one string; bracketing `allowed-tools`\nwould promote a string into a list. Either silently changes tool permissions. Both are pinned by\ndedicated ACs in TRD-002 and TRD-003.\nSatisfies: REQ-002\nPRD ACs: AC-002-1, AC-003-1\nTarget File: \nActions:\n\nImplementation AC:\n\nDependencies: TRD-001","status":"open","priority":2,"issue_type":"task","created_at":"2026-08-07T20:00:51.528511600Z","created_by":"unknown","updated_at":"2026-08-07T20:01:01.129569700Z","source_repo":"ensemble","source_repo_path":"\\\\?\\C:\\dev\\ensemble","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"br-mb5","depends_on_id":"br-ieq","type":"blocks","created_at":"2026-08-07T20:01:01.129338500Z","created_by":"unknown","metadata":"{}","thread_id":""}]}
{"id":"br-490","title":"[trd:trd-2026-6a87c77f-frontmatter-yaml-escaping:task:TRD-003-TEST] - [ ] **TRD-003-TEST** Unit tests for agent frontmatter emission (0.5h) ` `","description":"## Test Task: TRD-003-TEST\nTRD Reference: docs/TRD/TRD-2026-6a87c77f-frontmatter-yaml-escaping.md#trd-003-test\nPRD Reference: docs/PRD/PRD-2026-6a87c77f-frontmatter-yaml-escaping.md#req-002\n\n--- TRD Context ---\n### 1.1 Chosen approach — Option A: `JSON.stringify` scalar helper at the existing emit seam\n\nKeep the hand-built frontmatter lines in both transformers. Introduce one helper that folds a value\nto a single line and quotes it via `JSON.stringify`, and route every emitted value through it.\nSeparately, add a parse check at `generateMarkdown()` — the single funnel every artifact already\npasses through — and reuse that same check in `validate-all.js`.\n\n```js\n// scripts/lib/yaml-scalar.js\nconst foldScalar  = (v) => String(v).replace(/\\s+/g, ' ').trim();\nconst yamlScalar  = (v) => JSON.stringify(foldScalar(v));\n```\n\n**Why `JSON.stringify`:** a JSON string literal is a valid YAML 1.2 double-quoted scalar. The\nescaping is therefore stdlib's, not ours — there is no hand-rolled quoting logic to get wrong, and\nno \"does this need quoting?\" predicate. That predicate is precisely the defect in `packages/pi`\n(PRD REQ-008).\n\n**Verified** against eight adversarial inputs — mid-string `: `, bracket group with trailing\ncontent, embedded newlines and blank lines, embedded double quotes and backslashes, a leading `*`\nwith `#`/`|`/`>`/`%`/`&`/`!` throughout, a bare `ensemble:feature`, leading/trailing whitespace,\nand non-ASCII. All eight round-trip through `yaml.load` to the folded original exactly.\n\n### 1.2 Alternatives considered\n\n**Option B — serialize the frontmatter block with `js-yaml`'s `dump()`.**\nCorrect by construction with none of our own quoting logic, and measured to produce a *smaller*\ndiff than Option A (js-yaml quotes only when necessary, so previously-valid artifacts emit\nbyte-identically). Rejected on emitter-style coupling, measured empirically:\n\n| Behaviour | `dump()` output | Consequence |\n|---|---|---|\n| Multi-line description | emits a `\\|` block scalar | valid YAML, but violates REQ-003's single-line requirement; needs pre-folding anyway |\n| `tools: [Read, Write]` | restyled to a block sequence (`- Read`) | changes every agent artifact's shape |\n| `last-updated: 2026-03-15` | newly quoted | js-yaml decides, we don't |\n\nEach is individually tunable, but the result is our code negotiating with a general-purpose\nemitter's heuristics for a seven-key flat map. Option A has no heuristics at all.\n\n> **PRD correction:** PRD v1.0.1's non-goal states `yaml.dump()` would produce \"a much noisier\n> diff.\" That rationale is wrong — measurement shows the opposite. The conclusion (don't use it)\n> still holds, on the emitter-coupling grounds above. PRD corrected to match in v1.0.2.\n\n**Option C — inline the quoting separately in each transformer.** Rejected by PRD REQ-001: three\nemitters already exist and `packages/pi`'s copy has already drifted into incorrectness. One helper,\none rule.\n\n### 1.3 Two shapes that must not be conflated\n\nThe single highest-risk detail in this change. Two frontmatter keys look similar and are not:\n\n| Key | Current emission | YAML type | Correct treatment |\n|---|---|---|---|\n| `allowed-tools` (command) | `allowed_tools.join(', ')` | **string** — `\"Read, Write\"` | `yamlScalar(joined)` — quote the whole joined string |\n| `tools` (agent) | `[${tools.join(', ')}]` | **flow sequence** — `[\"Read\",\"Write\"]` | `[${tools.map(yamlScalar).join(', ')}]` — quote each element |\n\nQuoting `tools` as a whole string would collapse a list into one string; bracketing `allowed-tools`\nwould promote a string into a list. Either silently changes tool permissions. Both are pinned by\ndedicated ACs in TRD-002 and TRD-003.\nVerifies Task: docs/TRD/TRD-2026-6a87c77f-frontmatter-yaml-escaping.md#trd-003\nVerifies: TRD-003\nSatisfies: REQ-002\nPRD ACs Proven: AC-002-1, AC-003-1\nProof of requirement: \nTarget Files: \nActions:\n\nTest AC:\n\nDependencies: TRD-003","status":"open","priority":2,"issue_type":"task","created_at":"2026-08-07T20:00:51.721524100Z","created_by":"unknown","updated_at":"2026-08-07T20:01:01.424076100Z","source_repo":"ensemble","source_repo_path":"\\\\?\\C:\\dev\\ensemble","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"br-490","depends_on_id":"br-mb5","type":"blocks","created_at":"2026-08-07T20:01:01.423841200Z","created_by":"unknown","metadata":"{}","thread_id":""}]}
{"id":"br-cds","title":"[trd:trd-2026-6a87c77f-frontmatter-yaml-escaping:task:TRD-004] - [ ] **TRD-004** Create `scripts/lib/frontmatter-check.js` with `extractFrontmatter` and `checkFrontmatter` (1h) ` `","description":"## Task: TRD-004\nTRD Reference: docs/TRD/TRD-2026-6a87c77f-frontmatter-yaml-escaping.md#trd-004\nPRD Reference: docs/PRD/PRD-2026-6a87c77f-frontmatter-yaml-escaping.md#req-004\n\n--- TRD Context ---\n### 1.1 Chosen approach — Option A: `JSON.stringify` scalar helper at the existing emit seam\n\nKeep the hand-built frontmatter lines in both transformers. Introduce one helper that folds a value\nto a single line and quotes it via `JSON.stringify`, and route every emitted value through it.\nSeparately, add a parse check at `generateMarkdown()` — the single funnel every artifact already\npasses through — and reuse that same check in `validate-all.js`.\n\n```js\n// scripts/lib/yaml-scalar.js\nconst foldScalar  = (v) => String(v).replace(/\\s+/g, ' ').trim();\nconst yamlScalar  = (v) => JSON.stringify(foldScalar(v));\n```\n\n**Why `JSON.stringify`:** a JSON string literal is a valid YAML 1.2 double-quoted scalar. The\nescaping is therefore stdlib's, not ours — there is no hand-rolled quoting logic to get wrong, and\nno \"does this need quoting?\" predicate. That predicate is precisely the defect in `packages/pi`\n(PRD REQ-008).\n\n**Verified** against eight adversarial inputs — mid-string `: `, bracket group with trailing\ncontent, embedded newlines and blank lines, embedded double quotes and backslashes, a leading `*`\nwith `#`/`|`/`>`/`%`/`&`/`!` throughout, a bare `ensemble:feature`, leading/trailing whitespace,\nand non-ASCII. All eight round-trip through `yaml.load` to the folded original exactly.\n\n### 1.2 Alternatives considered\n\n**Option B — serialize the frontmatter block with `js-yaml`'s `dump()`.**\nCorrect by construction with none of our own quoting logic, and measured to produce a *smaller*\ndiff than Option A (js-yaml quotes only when necessary, so previously-valid artifacts emit\nbyte-identically). Rejected on emitter-style coupling, measured empirically:\n\n| Behaviour | `dump()` output | Consequence |\n|---|---|---|\n| Multi-line description | emits a `\\|` block scalar | valid YAML, but violates REQ-003's single-line requirement; needs pre-folding anyway |\n| `tools: [Read, Write]` | restyled to a block sequence (`- Read`) | changes every agent artifact's shape |\n| `last-updated: 2026-03-15` | newly quoted | js-yaml decides, we don't |\n\nEach is individually tunable, but the result is our code negotiating with a general-purpose\nemitter's heuristics for a seven-key flat map. Option A has no heuristics at all.\n\n> **PRD correction:** PRD v1.0.1's non-goal states `yaml.dump()` would produce \"a much noisier\n> diff.\" That rationale is wrong — measurement shows the opposite. The conclusion (don't use it)\n> still holds, on the emitter-coupling grounds above. PRD corrected to match in v1.0.2.\n\n**Option C — inline the quoting separately in each transformer.** Rejected by PRD REQ-001: three\nemitters already exist and `packages/pi`'s copy has already drifted into incorrectness. One helper,\none rule.\n\n### 1.3 Two shapes that must not be conflated\n\nThe single highest-risk detail in this change. Two frontmatter keys look similar and are not:\n\n| Key | Current emission | YAML type | Correct treatment |\n|---|---|---|---|\n| `allowed-tools` (command) | `allowed_tools.join(', ')` | **string** — `\"Read, Write\"` | `yamlScalar(joined)` — quote the whole joined string |\n| `tools` (agent) | `[${tools.join(', ')}]` | **flow sequence** — `[\"Read\",\"Write\"]` | `[${tools.map(yamlScalar).join(', ')}]` — quote each element |\n\nQuoting `tools` as a whole string would collapse a list into one string; bracketing `allowed-tools`\nwould promote a string into a list. Either silently changes tool permissions. Both are pinned by\ndedicated ACs in TRD-002 and TRD-003.\nSatisfies: REQ-004\nPRD ACs: AC-004-1, AC-006-1\nTarget File: \nActions:\n\nImplementation AC:\n\nDependencies: ","status":"open","priority":2,"issue_type":"task","created_at":"2026-08-07T20:00:51.940472300Z","created_by":"unknown","updated_at":"2026-08-07T20:00:51.940472300Z","source_repo":"ensemble","source_repo_path":"\\\\?\\C:\\dev\\ensemble","compaction_level":0,"original_size":0}
{"id":"br-tq8","title":"[trd:trd-2026-6a87c77f-frontmatter-yaml-escaping:task:TRD-004-TEST] - [ ] **TRD-004-TEST** Unit tests for the check, one per PRD defect class (0.5h) ` `","description":"## Test Task: TRD-004-TEST\nTRD Reference: docs/TRD/TRD-2026-6a87c77f-frontmatter-yaml-escaping.md#trd-004-test\nPRD Reference: docs/PRD/PRD-2026-6a87c77f-frontmatter-yaml-escaping.md#req-004\n\n--- TRD Context ---\n### 1.1 Chosen approach — Option A: `JSON.stringify` scalar helper at the existing emit seam\n\nKeep the hand-built frontmatter lines in both transformers. Introduce one helper that folds a value\nto a single line and quotes it via `JSON.stringify`, and route every emitted value through it.\nSeparately, add a parse check at `generateMarkdown()` — the single funnel every artifact already\npasses through — and reuse that same check in `validate-all.js`.\n\n```js\n// scripts/lib/yaml-scalar.js\nconst foldScalar  = (v) => String(v).replace(/\\s+/g, ' ').trim();\nconst yamlScalar  = (v) => JSON.stringify(foldScalar(v));\n```\n\n**Why `JSON.stringify`:** a JSON string literal is a valid YAML 1.2 double-quoted scalar. The\nescaping is therefore stdlib's, not ours — there is no hand-rolled quoting logic to get wrong, and\nno \"does this need quoting?\" predicate. That predicate is precisely the defect in `packages/pi`\n(PRD REQ-008).\n\n**Verified** against eight adversarial inputs — mid-string `: `, bracket group with trailing\ncontent, embedded newlines and blank lines, embedded double quotes and backslashes, a leading `*`\nwith `#`/`|`/`>`/`%`/`&`/`!` throughout, a bare `ensemble:feature`, leading/trailing whitespace,\nand non-ASCII. All eight round-trip through `yaml.load` to the folded original exactly.\n\n### 1.2 Alternatives considered\n\n**Option B — serialize the frontmatter block with `js-yaml`'s `dump()`.**\nCorrect by construction with none of our own quoting logic, and measured to produce a *smaller*\ndiff than Option A (js-yaml quotes only when necessary, so previously-valid artifacts emit\nbyte-identically). Rejected on emitter-style coupling, measured empirically:\n\n| Behaviour | `dump()` output | Consequence |\n|---|---|---|\n| Multi-line description | emits a `\\|` block scalar | valid YAML, but violates REQ-003's single-line requirement; needs pre-folding anyway |\n| `tools: [Read, Write]` | restyled to a block sequence (`- Read`) | changes every agent artifact's shape |\n| `last-updated: 2026-03-15` | newly quoted | js-yaml decides, we don't |\n\nEach is individually tunable, but the result is our code negotiating with a general-purpose\nemitter's heuristics for a seven-key flat map. Option A has no heuristics at all.\n\n> **PRD correction:** PRD v1.0.1's non-goal states `yaml.dump()` would produce \"a much noisier\n> diff.\" That rationale is wrong — measurement shows the opposite. The conclusion (don't use it)\n> still holds, on the emitter-coupling grounds above. PRD corrected to match in v1.0.2.\n\n**Option C — inline the quoting separately in each transformer.** Rejected by PRD REQ-001: three\nemitters already exist and `packages/pi`'s copy has already drifted into incorrectness. One helper,\none rule.\n\n### 1.3 Two shapes that must not be conflated\n\nThe single highest-risk detail in this change. Two frontmatter keys look similar and are not:\n\n| Key | Current emission | YAML type | Correct treatment |\n|---|---|---|---|\n| `allowed-tools` (command) | `allowed_tools.join(', ')` | **string** — `\"Read, Write\"` | `yamlScalar(joined)` — quote the whole joined string |\n| `tools` (agent) | `[${tools.join(', ')}]` | **flow sequence** — `[\"Read\",\"Write\"]` | `[${tools.map(yamlScalar).join(', ')}]` — quote each element |\n\nQuoting `tools` as a whole string would collapse a list into one string; bracketing `allowed-tools`\nwould promote a string into a list. Either silently changes tool permissions. Both are pinned by\ndedicated ACs in TRD-002 and TRD-003.\nVerifies Task: docs/TRD/TRD-2026-6a87c77f-frontmatter-yaml-escaping.md#trd-004\nVerifies: TRD-004\nSatisfies: REQ-004\nPRD ACs Proven: AC-004-1, AC-006-1\nProof of requirement: \nTarget Files: \nActions:\n\nTest AC:\n\nDependencies: TRD-004","status":"open","priority":2,"issue_type":"task","created_at":"2026-08-07T20:00:52.176970900Z","created_by":"unknown","updated_at":"2026-08-07T20:01:01.707818400Z","source_repo":"ensemble","source_repo_path":"\\\\?\\C:\\dev\\ensemble","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"br-tq8","depends_on_id":"br-cds","type":"blocks","created_at":"2026-08-07T20:01:01.707551Z","created_by":"unknown","metadata":"{}","thread_id":""}]}
{"id":"br-fg9","title":"[trd:trd-2026-6a87c77f-frontmatter-yaml-escaping:task:TRD-005] - [ ] **TRD-005** Call `checkFrontmatter` inside `generateMarkdown()` before returning (0.5h) ` `","description":"## Task: TRD-005\nTRD Reference: docs/TRD/TRD-2026-6a87c77f-frontmatter-yaml-escaping.md#trd-005\nPRD Reference: docs/PRD/PRD-2026-6a87c77f-frontmatter-yaml-escaping.md#req-004\n\n--- TRD Context ---\n### 1.1 Chosen approach — Option A: `JSON.stringify` scalar helper at the existing emit seam\n\nKeep the hand-built frontmatter lines in both transformers. Introduce one helper that folds a value\nto a single line and quotes it via `JSON.stringify`, and route every emitted value through it.\nSeparately, add a parse check at `generateMarkdown()` — the single funnel every artifact already\npasses through — and reuse that same check in `validate-all.js`.\n\n```js\n// scripts/lib/yaml-scalar.js\nconst foldScalar  = (v) => String(v).replace(/\\s+/g, ' ').trim();\nconst yamlScalar  = (v) => JSON.stringify(foldScalar(v));\n```\n\n**Why `JSON.stringify`:** a JSON string literal is a valid YAML 1.2 double-quoted scalar. The\nescaping is therefore stdlib's, not ours — there is no hand-rolled quoting logic to get wrong, and\nno \"does this need quoting?\" predicate. That predicate is precisely the defect in `packages/pi`\n(PRD REQ-008).\n\n**Verified** against eight adversarial inputs — mid-string `: `, bracket group with trailing\ncontent, embedded newlines and blank lines, embedded double quotes and backslashes, a leading `*`\nwith `#`/`|`/`>`/`%`/`&`/`!` throughout, a bare `ensemble:feature`, leading/trailing whitespace,\nand non-ASCII. All eight round-trip through `yaml.load` to the folded original exactly.\n\n### 1.2 Alternatives considered\n\n**Option B — serialize the frontmatter block with `js-yaml`'s `dump()`.**\nCorrect by construction with none of our own quoting logic, and measured to produce a *smaller*\ndiff than Option A (js-yaml quotes only when necessary, so previously-valid artifacts emit\nbyte-identically). Rejected on emitter-style coupling, measured empirically:\n\n| Behaviour | `dump()` output | Consequence |\n|---|---|---|\n| Multi-line description | emits a `\\|` block scalar | valid YAML, but violates REQ-003's single-line requirement; needs pre-folding anyway |\n| `tools: [Read, Write]` | restyled to a block sequence (`- Read`) | changes every agent artifact's shape |\n| `last-updated: 2026-03-15` | newly quoted | js-yaml decides, we don't |\n\nEach is individually tunable, but the result is our code negotiating with a general-purpose\nemitter's heuristics for a seven-key flat map. Option A has no heuristics at all.\n\n> **PRD correction:** PRD v1.0.1's non-goal states `yaml.dump()` would produce \"a much noisier\n> diff.\" That rationale is wrong — measurement shows the opposite. The conclusion (don't use it)\n> still holds, on the emitter-coupling grounds above. PRD corrected to match in v1.0.2.\n\n**Option C — inline the quoting separately in each transformer.** Rejected by PRD REQ-001: three\nemitters already exist and `packages/pi`'s copy has already drifted into incorrectness. One helper,\none rule.\n\n### 1.3 Two shapes that must not be conflated\n\nThe single highest-risk detail in this change. Two frontmatter keys look similar and are not:\n\n| Key | Current emission | YAML type | Correct treatment |\n|---|---|---|---|\n| `allowed-tools` (command) | `allowed_tools.join(', ')` | **string** — `\"Read, Write\"` | `yamlScalar(joined)` — quote the whole joined string |\n| `tools` (agent) | `[${tools.join(', ')}]` | **flow sequence** — `[\"Read\",\"Write\"]` | `[${tools.map(yamlScalar).join(', ')}]` — quote each element |\n\nQuoting `tools` as a whole string would collapse a list into one string; bracketing `allowed-tools`\nwould promote a string into a list. Either silently changes tool permissions. Both are pinned by\ndedicated ACs in TRD-002 and TRD-003.\nSatisfies: REQ-004\nPRD ACs: AC-004-1, AC-004-2\nTarget File: \nActions:\n\nImplementation AC:\n\nDependencies: TRD-004","status":"open","priority":2,"issue_type":"task","created_at":"2026-08-07T20:00:52.424697500Z","created_by":"unknown","updated_at":"2026-08-07T20:01:01.985116Z","source_repo":"ensemble","source_repo_path":"\\\\?\\C:\\dev\\ensemble","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"br-fg9","depends_on_id":"br-cds","type":"blocks","created_at":"2026-08-07T20:01:01.984872300Z","created_by":"unknown","metadata":"{}","thread_id":""}]}
{"id":"br-ioo","title":"[trd:trd-2026-6a87c77f-frontmatter-yaml-escaping:task:TRD-005-TEST] - [ ] **TRD-005-TEST** Test that generation fails closed (0.5h) ` `","description":"## Test Task: TRD-005-TEST\nTRD Reference: docs/TRD/TRD-2026-6a87c77f-frontmatter-yaml-escaping.md#trd-005-test\nPRD Reference: docs/PRD/PRD-2026-6a87c77f-frontmatter-yaml-escaping.md#req-004\n\n--- TRD Context ---\n### 1.1 Chosen approach — Option A: `JSON.stringify` scalar helper at the existing emit seam\n\nKeep the hand-built frontmatter lines in both transformers. Introduce one helper that folds a value\nto a single line and quotes it via `JSON.stringify`, and route every emitted value through it.\nSeparately, add a parse check at `generateMarkdown()` — the single funnel every artifact already\npasses through — and reuse that same check in `validate-all.js`.\n\n```js\n// scripts/lib/yaml-scalar.js\nconst foldScalar  = (v) => String(v).replace(/\\s+/g, ' ').trim();\nconst yamlScalar  = (v) => JSON.stringify(foldScalar(v));\n```\n\n**Why `JSON.stringify`:** a JSON string literal is a valid YAML 1.2 double-quoted scalar. The\nescaping is therefore stdlib's, not ours — there is no hand-rolled quoting logic to get wrong, and\nno \"does this need quoting?\" predicate. That predicate is precisely the defect in `packages/pi`\n(PRD REQ-008).\n\n**Verified** against eight adversarial inputs — mid-string `: `, bracket group with trailing\ncontent, embedded newlines and blank lines, embedded double quotes and backslashes, a leading `*`\nwith `#`/`|`/`>`/`%`/`&`/`!` throughout, a bare `ensemble:feature`, leading/trailing whitespace,\nand non-ASCII. All eight round-trip through `yaml.load` to the folded original exactly.\n\n### 1.2 Alternatives considered\n\n**Option B — serialize the frontmatter block with `js-yaml`'s `dump()`.**\nCorrect by construction with none of our own quoting logic, and measured to produce a *smaller*\ndiff than Option A (js-yaml quotes only when necessary, so previously-valid artifacts emit\nbyte-identically). Rejected on emitter-style coupling, measured empirically:\n\n| Behaviour | `dump()` output | Consequence |\n|---|---|---|\n| Multi-line description | emits a `\\|` block scalar | valid YAML, but violates REQ-003's single-line requirement; needs pre-folding anyway |\n| `tools: [Read, Write]` | restyled to a block sequence (`- Read`) | changes every agent artifact's shape |\n| `last-updated: 2026-03-15` | newly quoted | js-yaml decides, we don't |\n\nEach is individually tunable, but the result is our code negotiating with a general-purpose\nemitter's heuristics for a seven-key flat map. Option A has no heuristics at all.\n\n> **PRD correction:** PRD v1.0.1's non-goal states `yaml.dump()` would produce \"a much noisier\n> diff.\" That rationale is wrong — measurement shows the opposite. The conclusion (don't use it)\n> still holds, on the emitter-coupling grounds above. PRD corrected to match in v1.0.2.\n\n**Option C — inline the quoting separately in each transformer.** Rejected by PRD REQ-001: three\nemitters already exist and `packages/pi`'s copy has already drifted into incorrectness. One helper,\none rule.\n\n### 1.3 Two shapes that must not be conflated\n\nThe single highest-risk detail in this change. Two frontmatter keys look similar and are not:\n\n| Key | Current emission | YAML type | Correct treatment |\n|---|---|---|---|\n| `allowed-tools` (command) | `allowed_tools.join(', ')` | **string** — `\"Read, Write\"` | `yamlScalar(joined)` — quote the whole joined string |\n| `tools` (agent) | `[${tools.join(', ')}]` | **flow sequence** — `[\"Read\",\"Write\"]` | `[${tools.map(yamlScalar).join(', ')}]` — quote each element |\n\nQuoting `tools` as a whole string would collapse a list into one string; bracketing `allowed-tools`\nwould promote a string into a list. Either silently changes tool permissions. Both are pinned by\ndedicated ACs in TRD-002 and TRD-003.\nVerifies Task: docs/TRD/TRD-2026-6a87c77f-frontmatter-yaml-escaping.md#trd-005\nVerifies: TRD-005\nSatisfies: REQ-004\nPRD ACs Proven: AC-004-1\nProof of requirement: \nTarget Files: \nActions:\n\nTest AC:\n\nDependencies: TRD-005","status":"open","priority":2,"issue_type":"task","created_at":"2026-08-07T20:00:52.661278800Z","created_by":"unknown","updated_at":"2026-08-07T20:01:02.264204700Z","source_repo":"ensemble","source_repo_path":"\\\\?\\C:\\dev\\ensemble","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"br-ioo","depends_on_id":"br-fg9","type":"blocks","created_at":"2026-08-07T20:01:02.263954100Z","created_by":"unknown","metadata":"{}","thread_id":""}]}
{"id":"br-6uy","title":"[trd:trd-2026-6a87c77f-frontmatter-yaml-escaping:task:TRD-006] - [ ] **TRD-006** Add a repo-wide `packages/**/*.md` frontmatter walk to `validate-all.js` (1h) ` `","description":"## Task: TRD-006\nTRD Reference: docs/TRD/TRD-2026-6a87c77f-frontmatter-yaml-escaping.md#trd-006\nPRD Reference: docs/PRD/PRD-2026-6a87c77f-frontmatter-yaml-escaping.md#req-005\n\n--- TRD Context ---\n### 1.1 Chosen approach — Option A: `JSON.stringify` scalar helper at the existing emit seam\n\nKeep the hand-built frontmatter lines in both transformers. Introduce one helper that folds a value\nto a single line and quotes it via `JSON.stringify`, and route every emitted value through it.\nSeparately, add a parse check at `generateMarkdown()` — the single funnel every artifact already\npasses through — and reuse that same check in `validate-all.js`.\n\n```js\n// scripts/lib/yaml-scalar.js\nconst foldScalar  = (v) => String(v).replace(/\\s+/g, ' ').trim();\nconst yamlScalar  = (v) => JSON.stringify(foldScalar(v));\n```\n\n**Why `JSON.stringify`:** a JSON string literal is a valid YAML 1.2 double-quoted scalar. The\nescaping is therefore stdlib's, not ours — there is no hand-rolled quoting logic to get wrong, and\nno \"does this need quoting?\" predicate. That predicate is precisely the defect in `packages/pi`\n(PRD REQ-008).\n\n**Verified** against eight adversarial inputs — mid-string `: `, bracket group with trailing\ncontent, embedded newlines and blank lines, embedded double quotes and backslashes, a leading `*`\nwith `#`/`|`/`>`/`%`/`&`/`!` throughout, a bare `ensemble:feature`, leading/trailing whitespace,\nand non-ASCII. All eight round-trip through `yaml.load` to the folded original exactly.\n\n### 1.2 Alternatives considered\n\n**Option B — serialize the frontmatter block with `js-yaml`'s `dump()`.**\nCorrect by construction with none of our own quoting logic, and measured to produce a *smaller*\ndiff than Option A (js-yaml quotes only when necessary, so previously-valid artifacts emit\nbyte-identically). Rejected on emitter-style coupling, measured empirically:\n\n| Behaviour | `dump()` output | Consequence |\n|---|---|---|\n| Multi-line description | emits a `\\|` block scalar | valid YAML, but violates REQ-003's single-line requirement; needs pre-folding anyway |\n| `tools: [Read, Write]` | restyled to a block sequence (`- Read`) | changes every agent artifact's shape |\n| `last-updated: 2026-03-15` | newly quoted | js-yaml decides, we don't |\n\nEach is individually tunable, but the result is our code negotiating with a general-purpose\nemitter's heuristics for a seven-key flat map. Option A has no heuristics at all.\n\n> **PRD correction:** PRD v1.0.1's non-goal states `yaml.dump()` would produce \"a much noisier\n> diff.\" That rationale is wrong — measurement shows the opposite. The conclusion (don't use it)\n> still holds, on the emitter-coupling grounds above. PRD corrected to match in v1.0.2.\n\n**Option C — inline the quoting separately in each transformer.** Rejected by PRD REQ-001: three\nemitters already exist and `packages/pi`'s copy has already drifted into incorrectness. One helper,\none rule.\n\n### 1.3 Two shapes that must not be conflated\n\nThe single highest-risk detail in this change. Two frontmatter keys look similar and are not:\n\n| Key | Current emission | YAML type | Correct treatment |\n|---|---|---|---|\n| `allowed-tools` (command) | `allowed_tools.join(', ')` | **string** — `\"Read, Write\"` | `yamlScalar(joined)` — quote the whole joined string |\n| `tools` (agent) | `[${tools.join(', ')}]` | **flow sequence** — `[\"Read\",\"Write\"]` | `[${tools.map(yamlScalar).join(', ')}]` — quote each element |\n\nQuoting `tools` as a whole string would collapse a list into one string; bracketing `allowed-tools`\nwould promote a string into a list. Either silently changes tool permissions. Both are pinned by\ndedicated ACs in TRD-002 and TRD-003.\nSatisfies: REQ-005\nPRD ACs: AC-005-1, AC-005-2, AC-005-3\nTarget File: \nActions:\n\nImplementation AC:\n\nDependencies: TRD-004","status":"open","priority":2,"issue_type":"task","created_at":"2026-08-07T20:00:52.900735400Z","created_by":"unknown","updated_at":"2026-08-07T20:01:02.593222400Z","source_repo":"ensemble","source_repo_path":"\\\\?\\C:\\dev\\ensemble","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"br-6uy","depends_on_id":"br-cds","type":"blocks","created_at":"2026-08-07T20:01:02.592887200Z","created_by":"unknown","metadata":"{}","thread_id":""}]}
{"id":"br-ni5","title":"[trd:trd-2026-6a87c77f-frontmatter-yaml-escaping:task:TRD-006-TEST] - [ ] **TRD-006-TEST** Test the validate-side walk over a temp fixture tree (0.5h) ` `","description":"## Test Task: TRD-006-TEST\nTRD Reference: docs/TRD/TRD-2026-6a87c77f-frontmatter-yaml-escaping.md#trd-006-test\nPRD Reference: docs/PRD/PRD-2026-6a87c77f-frontmatter-yaml-escaping.md#req-005\n\n--- TRD Context ---\n### 1.1 Chosen approach — Option A: `JSON.stringify` scalar helper at the existing emit seam\n\nKeep the hand-built frontmatter lines in both transformers. Introduce one helper that folds a value\nto a single line and quotes it via `JSON.stringify`, and route every emitted value through it.\nSeparately, add a parse check at `generateMarkdown()` — the single funnel every artifact already\npasses through — and reuse that same check in `validate-all.js`.\n\n```js\n// scripts/lib/yaml-scalar.js\nconst foldScalar  = (v) => String(v).replace(/\\s+/g, ' ').trim();\nconst yamlScalar  = (v) => JSON.stringify(foldScalar(v));\n```\n\n**Why `JSON.stringify`:** a JSON string literal is a valid YAML 1.2 double-quoted scalar. The\nescaping is therefore stdlib's, not ours — there is no hand-rolled quoting logic to get wrong, and\nno \"does this need quoting?\" predicate. That predicate is precisely the defect in `packages/pi`\n(PRD REQ-008).\n\n**Verified** against eight adversarial inputs — mid-string `: `, bracket group with trailing\ncontent, embedded newlines and blank lines, embedded double quotes and backslashes, a leading `*`\nwith `#`/`|`/`>`/`%`/`&`/`!` throughout, a bare `ensemble:feature`, leading/trailing whitespace,\nand non-ASCII. All eight round-trip through `yaml.load` to the folded original exactly.\n\n### 1.2 Alternatives considered\n\n**Option B — serialize the frontmatter block with `js-yaml`'s `dump()`.**\nCorrect by construction with none of our own quoting logic, and measured to produce a *smaller*\ndiff than Option A (js-yaml quotes only when necessary, so previously-valid artifacts emit\nbyte-identically). Rejected on emitter-style coupling, measured empirically:\n\n| Behaviour | `dump()` output | Consequence |\n|---|---|---|\n| Multi-line description | emits a `\\|` block scalar | valid YAML, but violates REQ-003's single-line requirement; needs pre-folding anyway |\n| `tools: [Read, Write]` | restyled to a block sequence (`- Read`) | changes every agent artifact's shape |\n| `last-updated: 2026-03-15` | newly quoted | js-yaml decides, we don't |\n\nEach is individually tunable, but the result is our code negotiating with a general-purpose\nemitter's heuristics for a seven-key flat map. Option A has no heuristics at all.\n\n> **PRD correction:** PRD v1.0.1's non-goal states `yaml.dump()` would produce \"a much noisier\n> diff.\" That rationale is wrong — measurement shows the opposite. The conclusion (don't use it)\n> still holds, on the emitter-coupling grounds above. PRD corrected to match in v1.0.2.\n\n**Option C — inline the quoting separately in each transformer.** Rejected by PRD REQ-001: three\nemitters already exist and `packages/pi`'s copy has already drifted into incorrectness. One helper,\none rule.\n\n### 1.3 Two shapes that must not be conflated\n\nThe single highest-risk detail in this change. Two frontmatter keys look similar and are not:\n\n| Key | Current emission | YAML type | Correct treatment |\n|---|---|---|---|\n| `allowed-tools` (command) | `allowed_tools.join(', ')` | **string** — `\"Read, Write\"` | `yamlScalar(joined)` — quote the whole joined string |\n| `tools` (agent) | `[${tools.join(', ')}]` | **flow sequence** — `[\"Read\",\"Write\"]` | `[${tools.map(yamlScalar).join(', ')}]` — quote each element |\n\nQuoting `tools` as a whole string would collapse a list into one string; bracketing `allowed-tools`\nwould promote a string into a list. Either silently changes tool permissions. Both are pinned by\ndedicated ACs in TRD-002 and TRD-003.\nVerifies Task: docs/TRD/TRD-2026-6a87c77f-frontmatter-yaml-escaping.md#trd-006\nVerifies: TRD-006\nSatisfies: REQ-005\nPRD ACs Proven: AC-005-1, AC-005-3\nProof of requirement: \nTarget Files: \nActions:\n\nTest AC:\n\nDependencies: TRD-006","status":"open","priority":2,"issue_type":"task","created_at":"2026-08-07T20:00:53.099193900Z","created_by":"unknown","updated_at":"2026-08-07T20:01:02.941188900Z","source_repo":"ensemble","source_repo_path":"\\\\?\\C:\\dev\\ensemble","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"br-ni5","depends_on_id":"br-6uy","type":"blocks","created_at":"2026-08-07T20:01:02.940869400Z","created_by":"unknown","metadata":"{}","thread_id":""}]}
{"id":"br-asl","title":"[trd:trd-2026-6a87c77f-frontmatter-yaml-escaping:task:TRD-007] - [ ] **TRD-007** Regenerate all artifacts and verify the repro is clean (0.5h) ` `","description":"## Task: TRD-007\nTRD Reference: docs/TRD/TRD-2026-6a87c77f-frontmatter-yaml-escaping.md#trd-007\nPRD Reference: docs/PRD/PRD-2026-6a87c77f-frontmatter-yaml-escaping.md#req-007\n\n--- TRD Context ---\n### 1.1 Chosen approach — Option A: `JSON.stringify` scalar helper at the existing emit seam\n\nKeep the hand-built frontmatter lines in both transformers. Introduce one helper that folds a value\nto a single line and quotes it via `JSON.stringify`, and route every emitted value through it.\nSeparately, add a parse check at `generateMarkdown()` — the single funnel every artifact already\npasses through — and reuse that same check in `validate-all.js`.\n\n```js\n// scripts/lib/yaml-scalar.js\nconst foldScalar  = (v) => String(v).replace(/\\s+/g, ' ').trim();\nconst yamlScalar  = (v) => JSON.stringify(foldScalar(v));\n```\n\n**Why `JSON.stringify`:** a JSON string literal is a valid YAML 1.2 double-quoted scalar. The\nescaping is therefore stdlib's, not ours — there is no hand-rolled quoting logic to get wrong, and\nno \"does this need quoting?\" predicate. That predicate is precisely the defect in `packages/pi`\n(PRD REQ-008).\n\n**Verified** against eight adversarial inputs — mid-string `: `, bracket group with trailing\ncontent, embedded newlines and blank lines, embedded double quotes and backslashes, a leading `*`\nwith `#`/`|`/`>`/`%`/`&`/`!` throughout, a bare `ensemble:feature`, leading/trailing whitespace,\nand non-ASCII. All eight round-trip through `yaml.load` to the folded original exactly.\n\n### 1.2 Alternatives considered\n\n**Option B — serialize the frontmatter block with `js-yaml`'s `dump()`.**\nCorrect by construction with none of our own quoting logic, and measured to produce a *smaller*\ndiff than Option A (js-yaml quotes only when necessary, so previously-valid artifacts emit\nbyte-identically). Rejected on emitter-style coupling, measured empirically:\n\n| Behaviour | `dump()` output | Consequence |\n|---|---|---|\n| Multi-line description | emits a `\\|` block scalar | valid YAML, but violates REQ-003's single-line requirement; needs pre-folding anyway |\n| `tools: [Read, Write]` | restyled to a block sequence (`- Read`) | changes every agent artifact's shape |\n| `last-updated: 2026-03-15` | newly quoted | js-yaml decides, we don't |\n\nEach is individually tunable, but the result is our code negotiating with a general-purpose\nemitter's heuristics for a seven-key flat map. Option A has no heuristics at all.\n\n> **PRD correction:** PRD v1.0.1's non-goal states `yaml.dump()` would produce \"a much noisier\n> diff.\" That rationale is wrong — measurement shows the opposite. The conclusion (don't use it)\n> still holds, on the emitter-coupling grounds above. PRD corrected to match in v1.0.2.\n\n**Option C — inline the quoting separately in each transformer.** Rejected by PRD REQ-001: three\nemitters already exist and `packages/pi`'s copy has already drifted into incorrectness. One helper,\none rule.\n\n### 1.3 Two shapes that must not be conflated\n\nThe single highest-risk detail in this change. Two frontmatter keys look similar and are not:\n\n| Key | Current emission | YAML type | Correct treatment |\n|---|---|---|---|\n| `allowed-tools` (command) | `allowed_tools.join(', ')` | **string** — `\"Read, Write\"` | `yamlScalar(joined)` — quote the whole joined string |\n| `tools` (agent) | `[${tools.join(', ')}]` | **flow sequence** — `[\"Read\",\"Write\"]` | `[${tools.map(yamlScalar).join(', ')}]` — quote each element |\n\nQuoting `tools` as a whole string would collapse a list into one string; bracketing `allowed-tools`\nwould promote a string into a list. Either silently changes tool permissions. Both are pinned by\ndedicated ACs in TRD-002 and TRD-003.\nSatisfies: REQ-007\nPRD ACs: AC-007-1, AC-007-2, AC-009-1\nTarget File: \nActions:\n\nImplementation AC:\n\nDependencies: TRD-002, TRD-003, TRD-005","status":"open","priority":2,"issue_type":"task","created_at":"2026-08-07T20:00:53.298272600Z","created_by":"unknown","updated_at":"2026-08-07T20:01:03.892750700Z","source_repo":"ensemble","source_repo_path":"\\\\?\\C:\\dev\\ensemble","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"br-asl","depends_on_id":"br-ane","type":"blocks","created_at":"2026-08-07T20:01:03.275080500Z","created_by":"unknown","metadata":"{}","thread_id":""},{"issue_id":"br-asl","depends_on_id":"br-fg9","type":"blocks","created_at":"2026-08-07T20:01:03.892499800Z","created_by":"unknown","metadata":"{}","thread_id":""},{"issue_id":"br-asl","depends_on_id":"br-mb5","type":"blocks","created_at":"2026-08-07T20:01:03.618433100Z","created_by":"unknown","metadata":"{}","thread_id":""}]}
{"id":"br-dtd","title":"[trd:trd-2026-6a87c77f-frontmatter-yaml-escaping:task:TRD-007-TEST] - [ ] **TRD-007-TEST** Assert the regenerated tree is clean and body-stable (0.5h) ` `","description":"## Test Task: TRD-007-TEST\nTRD Reference: docs/TRD/TRD-2026-6a87c77f-frontmatter-yaml-escaping.md#trd-007-test\nPRD Reference: docs/PRD/PRD-2026-6a87c77f-frontmatter-yaml-escaping.md#req-007\n\n--- TRD Context ---\n### 1.1 Chosen approach — Option A: `JSON.stringify` scalar helper at the existing emit seam\n\nKeep the hand-built frontmatter lines in both transformers. Introduce one helper that folds a value\nto a single line and quotes it via `JSON.stringify`, and route every emitted value through it.\nSeparately, add a parse check at `generateMarkdown()` — the single funnel every artifact already\npasses through — and reuse that same check in `validate-all.js`.\n\n```js\n// scripts/lib/yaml-scalar.js\nconst foldScalar  = (v) => String(v).replace(/\\s+/g, ' ').trim();\nconst yamlScalar  = (v) => JSON.stringify(foldScalar(v));\n```\n\n**Why `JSON.stringify`:** a JSON string literal is a valid YAML 1.2 double-quoted scalar. The\nescaping is therefore stdlib's, not ours — there is no hand-rolled quoting logic to get wrong, and\nno \"does this need quoting?\" predicate. That predicate is precisely the defect in `packages/pi`\n(PRD REQ-008).\n\n**Verified** against eight adversarial inputs — mid-string `: `, bracket group with trailing\ncontent, embedded newlines and blank lines, embedded double quotes and backslashes, a leading `*`\nwith `#`/`|`/`>`/`%`/`&`/`!` throughout, a bare `ensemble:feature`, leading/trailing whitespace,\nand non-ASCII. All eight round-trip through `yaml.load` to the folded original exactly.\n\n### 1.2 Alternatives considered\n\n**Option B — serialize the frontmatter block with `js-yaml`'s `dump()`.**\nCorrect by construction with none of our own quoting logic, and measured to produce a *smaller*\ndiff than Option A (js-yaml quotes only when necessary, so previously-valid artifacts emit\nbyte-identically). Rejected on emitter-style coupling, measured empirically:\n\n| Behaviour | `dump()` output | Consequence |\n|---|---|---|\n| Multi-line description | emits a `\\|` block scalar | valid YAML, but violates REQ-003's single-line requirement; needs pre-folding anyway |\n| `tools: [Read, Write]` | restyled to a block sequence (`- Read`) | changes every agent artifact's shape |\n| `last-updated: 2026-03-15` | newly quoted | js-yaml decides, we don't |\n\nEach is individually tunable, but the result is our code negotiating with a general-purpose\nemitter's heuristics for a seven-key flat map. Option A has no heuristics at all.\n\n> **PRD correction:** PRD v1.0.1's non-goal states `yaml.dump()` would produce \"a much noisier\n> diff.\" That rationale is wrong — measurement shows the opposite. The conclusion (don't use it)\n> still holds, on the emitter-coupling grounds above. PRD corrected to match in v1.0.2.\n\n**Option C — inline the quoting separately in each transformer.** Rejected by PRD REQ-001: three\nemitters already exist and `packages/pi`'s copy has already drifted into incorrectness. One helper,\none rule.\n\n### 1.3 Two shapes that must not be conflated\n\nThe single highest-risk detail in this change. Two frontmatter keys look similar and are not:\n\n| Key | Current emission | YAML type | Correct treatment |\n|---|---|---|---|\n| `allowed-tools` (command) | `allowed_tools.join(', ')` | **string** — `\"Read, Write\"` | `yamlScalar(joined)` — quote the whole joined string |\n| `tools` (agent) | `[${tools.join(', ')}]` | **flow sequence** — `[\"Read\",\"Write\"]` | `[${tools.map(yamlScalar).join(', ')}]` — quote each element |\n\nQuoting `tools` as a whole string would collapse a list into one string; bracketing `allowed-tools`\nwould promote a string into a list. Either silently changes tool permissions. Both are pinned by\ndedicated ACs in TRD-002 and TRD-003.\nVerifies Task: docs/TRD/TRD-2026-6a87c77f-frontmatter-yaml-escaping.md#trd-007\nVerifies: TRD-007\nSatisfies: REQ-007\nPRD ACs Proven: AC-007-1, AC-009-1\nProof of requirement: \nTarget Files: \nActions:\n\nTest AC:\n\nDependencies: TRD-007","status":"open","priority":2,"issue_type":"task","created_at":"2026-08-07T20:00:53.492763300Z","created_by":"unknown","updated_at":"2026-08-07T20:01:04.174757200Z","source_repo":"ensemble","source_repo_path":"\\\\?\\C:\\dev\\ensemble","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"br-dtd","depends_on_id":"br-asl","type":"blocks","created_at":"2026-08-07T20:01:04.174460Z","created_by":"unknown","metadata":"{}","thread_id":""}]}
{"id":"br-9fi","title":"[trd:trd-2026-6a87c77f-frontmatter-yaml-escaping:task:TRD-008] - [ ] **TRD-008** Confirm the `last-updated` Date→string type change breaks no consumer (0.5h) ` `","description":"## Task: TRD-008\nTRD Reference: docs/TRD/TRD-2026-6a87c77f-frontmatter-yaml-escaping.md#trd-008\nPRD Reference: docs/PRD/PRD-2026-6a87c77f-frontmatter-yaml-escaping.md#req-009\n\n--- TRD Context ---\n### 1.1 Chosen approach — Option A: `JSON.stringify` scalar helper at the existing emit seam\n\nKeep the hand-built frontmatter lines in both transformers. Introduce one helper that folds a value\nto a single line and quotes it via `JSON.stringify`, and route every emitted value through it.\nSeparately, add a parse check at `generateMarkdown()` — the single funnel every artifact already\npasses through — and reuse that same check in `validate-all.js`.\n\n```js\n// scripts/lib/yaml-scalar.js\nconst foldScalar  = (v) => String(v).replace(/\\s+/g, ' ').trim();\nconst yamlScalar  = (v) => JSON.stringify(foldScalar(v));\n```\n\n**Why `JSON.stringify`:** a JSON string literal is a valid YAML 1.2 double-quoted scalar. The\nescaping is therefore stdlib's, not ours — there is no hand-rolled quoting logic to get wrong, and\nno \"does this need quoting?\" predicate. That predicate is precisely the defect in `packages/pi`\n(PRD REQ-008).\n\n**Verified** against eight adversarial inputs — mid-string `: `, bracket group with trailing\ncontent, embedded newlines and blank lines, embedded double quotes and backslashes, a leading `*`\nwith `#`/`|`/`>`/`%`/`&`/`!` throughout, a bare `ensemble:feature`, leading/trailing whitespace,\nand non-ASCII. All eight round-trip through `yaml.load` to the folded original exactly.\n\n### 1.2 Alternatives considered\n\n**Option B — serialize the frontmatter block with `js-yaml`'s `dump()`.**\nCorrect by construction with none of our own quoting logic, and measured to produce a *smaller*\ndiff than Option A (js-yaml quotes only when necessary, so previously-valid artifacts emit\nbyte-identically). Rejected on emitter-style coupling, measured empirically:\n\n| Behaviour | `dump()` output | Consequence |\n|---|---|---|\n| Multi-line description | emits a `\\|` block scalar | valid YAML, but violates REQ-003's single-line requirement; needs pre-folding anyway |\n| `tools: [Read, Write]` | restyled to a block sequence (`- Read`) | changes every agent artifact's shape |\n| `last-updated: 2026-03-15` | newly quoted | js-yaml decides, we don't |\n\nEach is individually tunable, but the result is our code negotiating with a general-purpose\nemitter's heuristics for a seven-key flat map. Option A has no heuristics at all.\n\n> **PRD correction:** PRD v1.0.1's non-goal states `yaml.dump()` would produce \"a much noisier\n> diff.\" That rationale is wrong — measurement shows the opposite. The conclusion (don't use it)\n> still holds, on the emitter-coupling grounds above. PRD corrected to match in v1.0.2.\n\n**Option C — inline the quoting separately in each transformer.** Rejected by PRD REQ-001: three\nemitters already exist and `packages/pi`'s copy has already drifted into incorrectness. One helper,\none rule.\n\n### 1.3 Two shapes that must not be conflated\n\nThe single highest-risk detail in this change. Two frontmatter keys look similar and are not:\n\n| Key | Current emission | YAML type | Correct treatment |\n|---|---|---|---|\n| `allowed-tools` (command) | `allowed_tools.join(', ')` | **string** — `\"Read, Write\"` | `yamlScalar(joined)` — quote the whole joined string |\n| `tools` (agent) | `[${tools.join(', ')}]` | **flow sequence** — `[\"Read\",\"Write\"]` | `[${tools.map(yamlScalar).join(', ')}]` — quote each element |\n\nQuoting `tools` as a whole string would collapse a list into one string; bracketing `allowed-tools`\nwould promote a string into a list. Either silently changes tool permissions. Both are pinned by\ndedicated ACs in TRD-002 and TRD-003.\nSatisfies: REQ-009\nPRD ACs: AC-009-1\nTarget File: \nActions:\n\nImplementation AC:\n\nDependencies: TRD-002","status":"open","priority":2,"issue_type":"task","created_at":"2026-08-07T20:00:53.685360500Z","created_by":"unknown","updated_at":"2026-08-07T20:01:04.469057500Z","source_repo":"ensemble","source_repo_path":"\\\\?\\C:\\dev\\ensemble","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"br-9fi","depends_on_id":"br-ane","type":"blocks","created_at":"2026-08-07T20:01:04.468785Z","created_by":"unknown","metadata":"{}","thread_id":""}]}
{"id":"br-4gs","title":"[trd:trd-2026-6a87c77f-frontmatter-yaml-escaping:task:TRD-009] - [ ] **TRD-009** Replace pi's `descSafe` predicate with the unconditional fold-and-quote rule in `packages/pi/src/transformers/agent-transformer.ts`; rebuild `dist/` (1h) ` `","description":"## Task: TRD-009\nTRD Reference: docs/TRD/TRD-2026-6a87c77f-frontmatter-yaml-escaping.md#trd-009\nPRD Reference: docs/PRD/PRD-2026-6a87c77f-frontmatter-yaml-escaping.md#req-008\n\n--- TRD Context ---\n### 1.1 Chosen approach — Option A: `JSON.stringify` scalar helper at the existing emit seam\n\nKeep the hand-built frontmatter lines in both transformers. Introduce one helper that folds a value\nto a single line and quotes it via `JSON.stringify`, and route every emitted value through it.\nSeparately, add a parse check at `generateMarkdown()` — the single funnel every artifact already\npasses through — and reuse that same check in `validate-all.js`.\n\n```js\n// scripts/lib/yaml-scalar.js\nconst foldScalar  = (v) => String(v).replace(/\\s+/g, ' ').trim();\nconst yamlScalar  = (v) => JSON.stringify(foldScalar(v));\n```\n\n**Why `JSON.stringify`:** a JSON string literal is a valid YAML 1.2 double-quoted scalar. The\nescaping is therefore stdlib's, not ours — there is no hand-rolled quoting logic to get wrong, and\nno \"does this need quoting?\" predicate. That predicate is precisely the defect in `packages/pi`\n(PRD REQ-008).\n\n**Verified** against eight adversarial inputs — mid-string `: `, bracket group with trailing\ncontent, embedded newlines and blank lines, embedded double quotes and backslashes, a leading `*`\nwith `#`/`|`/`>`/`%`/`&`/`!` throughout, a bare `ensemble:feature`, leading/trailing whitespace,\nand non-ASCII. All eight round-trip through `yaml.load` to the folded original exactly.\n\n### 1.2 Alternatives considered\n\n**Option B — serialize the frontmatter block with `js-yaml`'s `dump()`.**\nCorrect by construction with none of our own quoting logic, and measured to produce a *smaller*\ndiff than Option A (js-yaml quotes only when necessary, so previously-valid artifacts emit\nbyte-identically). Rejected on emitter-style coupling, measured empirically:\n\n| Behaviour | `dump()` output | Consequence |\n|---|---|---|\n| Multi-line description | emits a `\\|` block scalar | valid YAML, but violates REQ-003's single-line requirement; needs pre-folding anyway |\n| `tools: [Read, Write]` | restyled to a block sequence (`- Read`) | changes every agent artifact's shape |\n| `last-updated: 2026-03-15` | newly quoted | js-yaml decides, we don't |\n\nEach is individually tunable, but the result is our code negotiating with a general-purpose\nemitter's heuristics for a seven-key flat map. Option A has no heuristics at all.\n\n> **PRD correction:** PRD v1.0.1's non-goal states `yaml.dump()` would produce \"a much noisier\n> diff.\" That rationale is wrong — measurement shows the opposite. The conclusion (don't use it)\n> still holds, on the emitter-coupling grounds above. PRD corrected to match in v1.0.2.\n\n**Option C — inline the quoting separately in each transformer.** Rejected by PRD REQ-001: three\nemitters already exist and `packages/pi`'s copy has already drifted into incorrectness. One helper,\none rule.\n\n### 1.3 Two shapes that must not be conflated\n\nThe single highest-risk detail in this change. Two frontmatter keys look similar and are not:\n\n| Key | Current emission | YAML type | Correct treatment |\n|---|---|---|---|\n| `allowed-tools` (command) | `allowed_tools.join(', ')` | **string** — `\"Read, Write\"` | `yamlScalar(joined)` — quote the whole joined string |\n| `tools` (agent) | `[${tools.join(', ')}]` | **flow sequence** — `[\"Read\",\"Write\"]` | `[${tools.map(yamlScalar).join(', ')}]` — quote each element |\n\nQuoting `tools` as a whole string would collapse a list into one string; bracketing `allowed-tools`\nwould promote a string into a list. Either silently changes tool permissions. Both are pinned by\ndedicated ACs in TRD-002 and TRD-003.\nSatisfies: REQ-008\nPRD ACs: AC-008-1\nTarget File: \nActions:\n\nImplementation AC:\n\nDependencies: TRD-001","status":"open","priority":2,"issue_type":"task","created_at":"2026-08-07T20:00:53.874163700Z","created_by":"unknown","updated_at":"2026-08-07T20:01:04.745457900Z","source_repo":"ensemble","source_repo_path":"\\\\?\\C:\\dev\\ensemble","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"br-4gs","depends_on_id":"br-ieq","type":"blocks","created_at":"2026-08-07T20:01:04.745176300Z","created_by":"unknown","metadata":"{}","thread_id":""}]}
{"id":"br-d1l","title":"[trd:trd-2026-6a87c77f-frontmatter-yaml-escaping:task:TRD-009-TEST] - [ ] **TRD-009-TEST** Unit test for pi's agent frontmatter (0.5h) ` `","description":"## Test Task: TRD-009-TEST\nTRD Reference: docs/TRD/TRD-2026-6a87c77f-frontmatter-yaml-escaping.md#trd-009-test\nPRD Reference: docs/PRD/PRD-2026-6a87c77f-frontmatter-yaml-escaping.md#req-008\n\n--- TRD Context ---\n### 1.1 Chosen approach — Option A: `JSON.stringify` scalar helper at the existing emit seam\n\nKeep the hand-built frontmatter lines in both transformers. Introduce one helper that folds a value\nto a single line and quotes it via `JSON.stringify`, and route every emitted value through it.\nSeparately, add a parse check at `generateMarkdown()` — the single funnel every artifact already\npasses through — and reuse that same check in `validate-all.js`.\n\n```js\n// scripts/lib/yaml-scalar.js\nconst foldScalar  = (v) => String(v).replace(/\\s+/g, ' ').trim();\nconst yamlScalar  = (v) => JSON.stringify(foldScalar(v));\n```\n\n**Why `JSON.stringify`:** a JSON string literal is a valid YAML 1.2 double-quoted scalar. The\nescaping is therefore stdlib's, not ours — there is no hand-rolled quoting logic to get wrong, and\nno \"does this need quoting?\" predicate. That predicate is precisely the defect in `packages/pi`\n(PRD REQ-008).\n\n**Verified** against eight adversarial inputs — mid-string `: `, bracket group with trailing\ncontent, embedded newlines and blank lines, embedded double quotes and backslashes, a leading `*`\nwith `#`/`|`/`>`/`%`/`&`/`!` throughout, a bare `ensemble:feature`, leading/trailing whitespace,\nand non-ASCII. All eight round-trip through `yaml.load` to the folded original exactly.\n\n### 1.2 Alternatives considered\n\n**Option B — serialize the frontmatter block with `js-yaml`'s `dump()`.**\nCorrect by construction with none of our own quoting logic, and measured to produce a *smaller*\ndiff than Option A (js-yaml quotes only when necessary, so previously-valid artifacts emit\nbyte-identically). Rejected on emitter-style coupling, measured empirically:\n\n| Behaviour | `dump()` output | Consequence |\n|---|---|---|\n| Multi-line description | emits a `\\|` block scalar | valid YAML, but violates REQ-003's single-line requirement; needs pre-folding anyway |\n| `tools: [Read, Write]` | restyled to a block sequence (`- Read`) | changes every agent artifact's shape |\n| `last-updated: 2026-03-15` | newly quoted | js-yaml decides, we don't |\n\nEach is individually tunable, but the result is our code negotiating with a general-purpose\nemitter's heuristics for a seven-key flat map. Option A has no heuristics at all.\n\n> **PRD correction:** PRD v1.0.1's non-goal states `yaml.dump()` would produce \"a much noisier\n> diff.\" That rationale is wrong — measurement shows the opposite. The conclusion (don't use it)\n> still holds, on the emitter-coupling grounds above. PRD corrected to match in v1.0.2.\n\n**Option C — inline the quoting separately in each transformer.** Rejected by PRD REQ-001: three\nemitters already exist and `packages/pi`'s copy has already drifted into incorrectness. One helper,\none rule.\n\n### 1.3 Two shapes that must not be conflated\n\nThe single highest-risk detail in this change. Two frontmatter keys look similar and are not:\n\n| Key | Current emission | YAML type | Correct treatment |\n|---|---|---|---|\n| `allowed-tools` (command) | `allowed_tools.join(', ')` | **string** — `\"Read, Write\"` | `yamlScalar(joined)` — quote the whole joined string |\n| `tools` (agent) | `[${tools.join(', ')}]` | **flow sequence** — `[\"Read\",\"Write\"]` | `[${tools.map(yamlScalar).join(', ')}]` — quote each element |\n\nQuoting `tools` as a whole string would collapse a list into one string; bracketing `allowed-tools`\nwould promote a string into a list. Either silently changes tool permissions. Both are pinned by\ndedicated ACs in TRD-002 and TRD-003.\nVerifies Task: docs/TRD/TRD-2026-6a87c77f-frontmatter-yaml-escaping.md#trd-009\nVerifies: TRD-009\nSatisfies: REQ-008\nPRD ACs Proven: AC-008-1\nProof of requirement: \nTarget Files: \nActions:\n\nTest AC:\n\nDependencies: TRD-009","status":"open","priority":2,"issue_type":"task","created_at":"2026-08-07T20:00:54.075210100Z","created_by":"unknown","updated_at":"2026-08-07T20:01:05.028215500Z","source_repo":"ensemble","source_repo_path":"\\\\?\\C:\\dev\\ensemble","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"br-d1l","depends_on_id":"br-4gs","type":"blocks","created_at":"2026-08-07T20:01:05.027929800Z","created_by":"unknown","metadata":"{}","thread_id":""}]}
{"id":"br-yht","title":"[trd:trd-2026-6a87c77f-frontmatter-yaml-escaping:task:TRD-010] - [ ] **TRD-010** Full-suite regression confirmation (0.5h) ` `","description":"## Task: TRD-010\nTRD Reference: docs/TRD/TRD-2026-6a87c77f-frontmatter-yaml-escaping.md#trd-010\nPRD Reference: docs/PRD/PRD-2026-6a87c77f-frontmatter-yaml-escaping.md#req-010\n\n--- TRD Context ---\n### 1.1 Chosen approach — Option A: `JSON.stringify` scalar helper at the existing emit seam\n\nKeep the hand-built frontmatter lines in both transformers. Introduce one helper that folds a value\nto a single line and quotes it via `JSON.stringify`, and route every emitted value through it.\nSeparately, add a parse check at `generateMarkdown()` — the single funnel every artifact already\npasses through — and reuse that same check in `validate-all.js`.\n\n```js\n// scripts/lib/yaml-scalar.js\nconst foldScalar  = (v) => String(v).replace(/\\s+/g, ' ').trim();\nconst yamlScalar  = (v) => JSON.stringify(foldScalar(v));\n```\n\n**Why `JSON.stringify`:** a JSON string literal is a valid YAML 1.2 double-quoted scalar. The\nescaping is therefore stdlib's, not ours — there is no hand-rolled quoting logic to get wrong, and\nno \"does this need quoting?\" predicate. That predicate is precisely the defect in `packages/pi`\n(PRD REQ-008).\n\n**Verified** against eight adversarial inputs — mid-string `: `, bracket group with trailing\ncontent, embedded newlines and blank lines, embedded double quotes and backslashes, a leading `*`\nwith `#`/`|`/`>`/`%`/`&`/`!` throughout, a bare `ensemble:feature`, leading/trailing whitespace,\nand non-ASCII. All eight round-trip through `yaml.load` to the folded original exactly.\n\n### 1.2 Alternatives considered\n\n**Option B — serialize the frontmatter block with `js-yaml`'s `dump()`.**\nCorrect by construction with none of our own quoting logic, and measured to produce a *smaller*\ndiff than Option A (js-yaml quotes only when necessary, so previously-valid artifacts emit\nbyte-identically). Rejected on emitter-style coupling, measured empirically:\n\n| Behaviour | `dump()` output | Consequence |\n|---|---|---|\n| Multi-line description | emits a `\\|` block scalar | valid YAML, but violates REQ-003's single-line requirement; needs pre-folding anyway |\n| `tools: [Read, Write]` | restyled to a block sequence (`- Read`) | changes every agent artifact's shape |\n| `last-updated: 2026-03-15` | newly quoted | js-yaml decides, we don't |\n\nEach is individually tunable, but the result is our code negotiating with a general-purpose\nemitter's heuristics for a seven-key flat map. Option A has no heuristics at all.\n\n> **PRD correction:** PRD v1.0.1's non-goal states `yaml.dump()` would produce \"a much noisier\n> diff.\" That rationale is wrong — measurement shows the opposite. The conclusion (don't use it)\n> still holds, on the emitter-coupling grounds above. PRD corrected to match in v1.0.2.\n\n**Option C — inline the quoting separately in each transformer.** Rejected by PRD REQ-001: three\nemitters already exist and `packages/pi`'s copy has already drifted into incorrectness. One helper,\none rule.\n\n### 1.3 Two shapes that must not be conflated\n\nThe single highest-risk detail in this change. Two frontmatter keys look similar and are not:\n\n| Key | Current emission | YAML type | Correct treatment |\n|---|---|---|---|\n| `allowed-tools` (command) | `allowed_tools.join(', ')` | **string** — `\"Read, Write\"` | `yamlScalar(joined)` — quote the whole joined string |\n| `tools` (agent) | `[${tools.join(', ')}]` | **flow sequence** — `[\"Read\",\"Write\"]` | `[${tools.map(yamlScalar).join(', ')}]` — quote each element |\n\nQuoting `tools` as a whole string would collapse a list into one string; bracketing `allowed-tools`\nwould promote a string into a list. Either silently changes tool permissions. Both are pinned by\ndedicated ACs in TRD-002 and TRD-003.\nSatisfies: REQ-010\nPRD ACs: AC-010-1\nTarget File: \nActions:\n\nImplementation AC:\n\nDependencies: TRD-007, TRD-009","status":"open","priority":2,"issue_type":"task","created_at":"2026-08-07T20:00:54.272848300Z","created_by":"unknown","updated_at":"2026-08-07T20:01:05.599675900Z","source_repo":"ensemble","source_repo_path":"\\\\?\\C:\\dev\\ensemble","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"br-yht","depends_on_id":"br-4gs","type":"blocks","created_at":"2026-08-07T20:01:05.599430100Z","created_by":"unknown","metadata":"{}","thread_id":""},{"issue_id":"br-yht","depends_on_id":"br-asl","type":"blocks","created_at":"2026-08-07T20:01:05.320131200Z","created_by":"unknown","metadata":"{}","thread_id":""}]}
